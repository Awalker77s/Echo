# ===== Client (Vite) =====
# From Supabase project settings > API
VITE_SUPABASE_URL=
# From Supabase project settings > API (anon/public key)
VITE_SUPABASE_ANON_KEY=

# ===== Supabase Edge Functions =====
# Same as VITE_SUPABASE_URL but available to Deno runtime
SUPABASE_URL=
# From Supabase project settings > API (service role key; server-only secret)
SUPABASE_SERVICE_ROLE_KEY=

# ===== OpenAI =====
# From OpenAI dashboard > API Keys (server-side, used by Edge Functions)
OPENAI_API_KEY=
# Frontend-accessible OpenAI key (used by client-side idea extraction — VITE_ prefix required)
VITE_OPENAI_API_KEY=

# ===== Stripe =====
# From Stripe dashboard > Developers > API keys
STRIPE_SECRET_KEY=
# From Stripe dashboard > Developers > Webhooks (endpoint signing secret)
STRIPE_WEBHOOK_SECRET=
# From Stripe dashboard > Product price IDs for each plan
STRIPE_PRICE_CORE=
STRIPE_PRICE_MEMOIR=
STRIPE_PRICE_LIFETIME=
# Optional for frontend checkout wiring
STRIPE_PUBLISHABLE_KEY=

# Public app URL used in checkout redirect links
SITE_URL=http://localhost:5173

# ===== Optional integrations =====
# Resend API key from resend.com (for weekly summary emails)
RESEND_API_KEY=
# Verified sender address in Resend, e.g. Echo Journal <weekly@yourdomain.com>
RESEND_FROM_EMAIL=

# Print provider API keys (optional scaffolds)
PRINTFUL_API_KEY=
BLURB_API_KEY=
